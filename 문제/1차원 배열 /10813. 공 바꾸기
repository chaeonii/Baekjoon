a, b = map(int, input().split())
a_list = [str(i + 1) for i in range(a)]

for i in range(b):
    i, j = map(int, input().split())
    a_list[i-1], a_list[j-1] = a_list[j-1], a_list[i-1]

print(*a_lst)
